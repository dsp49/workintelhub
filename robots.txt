# robots.txt — WorkIntel Hub
# https://workintelhub.com
#
# One message, no contradictions: every crawler, including AI search and training crawlers,
# may read the site. (If Cloudflare's "managed robots.txt" / "block AI bots" is switched on in
# the dashboard it prepends its own Disallow rules ABOVE this file — turn it off there.)

User-agent: *
Allow: /

# Keep crawlers out of build/meta noise and tracking-parameter duplicates.
Disallow: /design/
Disallow: /*?utm_
Disallow: /*?fbclid

Sitemap: https://workintelhub.com/sitemap.xml
